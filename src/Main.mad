import IO from "IO"
import List from "List"
import Maybe from "Maybe"
import Random from "Random"
import String from "String"

import Box from "@/Ascii"



main = () => {
  Box.render(Box.basic("Hey this is a whole box!")) |> IO.putLine
}

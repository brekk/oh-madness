import { assertEquals, test } from "Test"

import { emoji, emojiGlobal } from "./EmojiRegex"
import RegExp from "./RegExp"




// test(
//   "emoji",
//   () => do {
//     _ <- assertEquals(RegExp.unsafeReplace(emojiGlobal, "👍👍👍", "👎"), "👎👎👎")
//     return assertEquals(RegExp.unsafeReplace(emoji, "🥺", "😘yes👏"), "🥺yes👏")
//   },
// )
